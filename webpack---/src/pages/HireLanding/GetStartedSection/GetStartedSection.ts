import Vue from 'vue'
import { VACANCY_ADD, FREELANCERS_LIST } from '@/constants-ts/routes'

export default Vue.extend({
  data () {
    return {
      VACANCY_ADD,
      FREELANCERS_LIST,
    }
  }
})
